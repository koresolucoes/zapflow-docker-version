# ============================================================
#  Configuração da Aplicação
# ============================================================
# URL base da aplicação, usada para gerar links e callbacks.
# Em desenvolvimento, geralmente é http://localhost:5173.
# Em produção, deve ser o seu domínio público.
VITE_APP_URL=http://localhost:5173
APP_URL=http://localhost:3001

# ============================================================
#  Configuração do Banco de Dados (PostgreSQL)
# ============================================================
# Usuário do banco de dados.
POSTGRES_USER=postgres
# Senha do banco de dados. Mude para algo seguro em produção.
POSTGRES_PASSWORD=postgres
# Nome do banco de dados a ser criado.
POSTGRES_DB=zapflow

# ============================================================
#  Configuração de Autenticação (JWT)
# ============================================================
# Chave secreta para assinar os tokens JWT.
# Deve ser uma string longa e aleatória.
# Você pode gerar uma com: openssl rand -hex 32
JWT_SECRET=your-super-secret-and-long-jwt-key

# ============================================================
#  Configuração do Redis
# ============================================================
# O host do Redis, geralmente 'redis' quando rodando com Docker Compose.
REDIS_HOST=redis
# A porta do Redis.
REDIS_PORT=6379

# ============================================================
#  Configurações Opcionais de IA (Google Gemini)
# ============================================================
# Chave da API do Google Gemini para funcionalidades de IA.
VITE_GEMINI_API_KEY=
